# Examples

| Example | What it shows |
| --- | --- |
| [`chat/`](./chat) | Interactive terminal chat: identity creation, seed discovery, sealed-box send, signed polling receive, on-disk cursor persistence. |

Examples import the SDK from `../../src` so they stay in sync with the source
and are covered by `npm run typecheck`. They run against the **live** BChat
network.
